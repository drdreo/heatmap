{
    "name": "docs",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview"
    },
    "dependencies": {
        "@drdreo/heatmap": "workspace:*"
    },
    "devDependencies": {
        "typescript": "~5.9.3",
        "vite": "npm:rolldown-vite@7.2.5"
    },
    "nx": {
        "targets": {
            "build": {
                "dependsOn": [
                    "@drdreo/heatmap:build"
                ]
            }
        }
    }
}
